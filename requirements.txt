# COTC Tactician - Dependencies
# Install with: pip install -r requirements.txt

# Core
pyyaml>=6.0.1
pydantic>=2.5.0

# Vector Database
chromadb>=0.4.22

# Embeddings
sentence-transformers>=2.2.2

# LLM Integration (choose one or both)
openai>=1.12.0          # For OpenAI API (GPT-4, etc.)
ollama>=0.1.7           # For local LLMs via Ollama

# CLI & Output
typer>=0.9.0
rich>=13.7.0

# MCP Server (for Cursor/Claude integration)
mcp[cli]>=1.2.0

# Development (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
